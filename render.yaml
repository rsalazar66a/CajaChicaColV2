services:
  - type: web
    name: caja-chica-financiera
    env: docker
    region: oregon
    plan: free
    dockerfilePath: Dockerfile
    dockerContext: .
    envVars:
      - key: FLASK_ENV
        value: production
    healthCheckPath: /api/health
